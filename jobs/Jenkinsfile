import groovy.json.JsonSlurperClassic

def jsonParse(def json) {
    new groovy.json.JsonSlurperClassic().parseText(json)
}
pipeline {
  agent { label 'master' }
  environment {
    appName = "variable" 
  }
  stages {

 stage("paso 1"){
     
      steps {
          script {			
           cmd "echo 'hola mundo'"
        }
      }
    }
  }
  post {
      always {          
          deleteDir()
           cmd "echo 'fase always'"
      }
      success {
            cmd "echo 'fase success'"
        }

      failure {
            cmd "echo 'fase failure'"
      }
      
  }
}  